# Keep large datasets and model caches away from the system drive.
# Change these paths if E: is unavailable on another machine.
VISIONSEEK_DATA_ROOT=/path/to/visionseek-data
HF_HOME=/path/to/huggingface-cache
TORCH_HOME=/path/to/torch-cache
